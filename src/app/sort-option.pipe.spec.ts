import { SortOptionPipe } from './sort-option.pipe';

describe('SortOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SortOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
